package com.dearmarcus.core;

public enum AiStatus {
    AVAILABLE,
    DOWNLOADABLE,
    DOWNLOADING,
    UNAVAILABLE,
    TOKEN_LIMIT,
    BUSY,
    QUOTA_EXCEEDED,
    BACKGROUND_BLOCKED,
    INVALID_OUTPUT,
    UNEXPECTED_ERROR
}
